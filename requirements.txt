# AI Visibility Explorer — Phase 1 (MVP) dependencies
# Recommended environment: Python 3.12 (matches the devcontainer / Codespaces image).
# Also verified working on Python 3.14; loosen the pins only if you need older Python.
pandas>=2.2
duckdb>=1.1
streamlit>=1.40
plotly>=5.24
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
python-dotenv>=1.0
pytest>=8.0
